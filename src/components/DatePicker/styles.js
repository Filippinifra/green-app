import styled from "styled-components";
import { COMMON_FIFTH_COLOR } from "constants/palette";

export const PickerWrapper = styled.View`
  border-bottom-width: 1px;
  border-color: ${COMMON_FIFTH_COLOR};
  flex: 1;
  width: 100%;
`;
